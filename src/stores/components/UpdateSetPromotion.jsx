import React from 'react';

const UpdateSetPromotion = ({set_promotion_id,storeId}) => {
  

  return (
    <div>
      <h2>แก้ไข Set Promotion</h2>
      {set_promotion_id}
      {storeId}
      
    </div>
  );
};

export default UpdateSetPromotion;
